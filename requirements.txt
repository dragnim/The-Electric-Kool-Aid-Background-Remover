# Requirements for The Electric Kool-Aid Background Remover v3.11
# Generated from a known-working environment:
#   Python 3.14.3 on Windows 11, AMD Ryzen 9 9950X3D, NVIDIA RTX 2080
#   Last verified: May 2026
#
# Install into a virtual environment:
#   py -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Then run:
#   py the-electric-kool-aid-background-remover.py
#
# Note: torch is the GPU build (CUDA 12.6), compatible with onnxruntime-gpu.
#       For CPU-only: pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
# Note: BEN2 is installed from a GitHub zip archive. Git is NOT required.
# Note: `transparent-background` (for InSPyReNet) is deliberately NOT listed
#       here. Its transitive dep chain (albumentations -> albucore ->
#       stringzilla) fails to build on Python 3.14 (no wheel + needs MSVC).
#       The app installs it lazily on first use of the InSPyReNet model, so
#       a failure there doesn't break the other six models. If you want
#       InSPyReNet and you're on Python 3.14, switch to Python 3.12.

# Core inference
torch==2.12.0
torchvision==0.27.0

# Background removal backends
rembg==2.0.75
ben2 @ https://github.com/PramaLLC/BEN2/archive/2c99a5da477b5523585bfa5c893888a6e818a8f6.zip

# Image handling
Pillow==12.2.0
opencv-python==4.13.0.92

# rembg dependencies (pinned for reproducibility)
onnxruntime-gpu==1.26.0  # GPU build; use onnxruntime==1.26.0 for CPU-only
PyMatting==1.1.15
scipy==1.17.1
scikit-image==0.26.0
numpy==2.4.4
tifffile==2026.5.2
pooch==1.9.0

# Drag and drop support
tkinterdnd2
